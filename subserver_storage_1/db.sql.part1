EQUENCE seq_editionsal_id
  START WITH 1
  INCREMENT BY 1
  NOCYCLE;

CREATE TABLE EMP_HISTORY (
  HISTORYDATE DATE,
  SAL NUMBER,
  STATUT NUMBER,
  EMPNO NUMBER,
  CONSTRAINT fk_emp_history_emp FOREIGN KEY (EMPNO) RE